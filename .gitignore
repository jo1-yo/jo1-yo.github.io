# dependencies
node_modules/

# local env / secrets — NEVER commit these (the phone number + Twilio creds live here)
.env
.env.local
.env*.local

# vercel
.vercel

# claude code local config
.claude/

# os
.DS_Store

# large original-image backups (local only, not deployed)
images/_originals_backup/
